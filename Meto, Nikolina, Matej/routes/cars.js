var express = require('express');
var router = express.Router();


/* GET users listing. */
router.get('/', function(req, res, next) {
  res.send('In progress page for cars');
  // res.render('cars', )
});





module.exports = router;
